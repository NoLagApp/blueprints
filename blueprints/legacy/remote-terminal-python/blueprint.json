{
  "blueprintId": "remote-terminal-python",
  "name": "Remote Terminal (Python Agent)",
  "description": "Python agent that exposes a local shell over NoLag for remote terminal access",
  "version": "1.0.0",
  "category": "devtools",
  "tags": ["terminal", "remote-access", "agent", "python", "pty", "devtools"],
  "difficulty": "advanced",
  "framework": null,
  "dependencies": {
    "websockets": ">=12.0",
    "msgpack": ">=1.0.0",
    "httpx": ">=0.27.0"
  },
  "config": {
    "topics": [
      {
        "name": "commands",
        "description": "Commands received from the web client (shell, session_start, session_input, etc.)"
      },
      {
        "name": "responses",
        "description": "Command responses sent back to the web client"
      },
      {
        "name": "status",
        "description": "Device status heartbeats broadcast every 30 seconds"
      },
      {
        "name": "session_output",
        "description": "Interactive PTY session output streamed to the web client"
      }
    ],
    "settings": {}
  },
  "readme": "# Remote Terminal (Python Agent)\n\n## Description\nA Python agent that runs on a target machine and exposes its shell over NoLag. Supports one-shot shell commands, interactive PTY sessions (via ptyprocess/pywinpty), file transfer, tab completion, and directory listing.\n\nThis is the **Python agent** component of a multi-part system. To use it, you also need the web client:\n- [remote-terminal-web](../remote-terminal-web/) -- Browser-based terminal client\n\nAlternative agents are available in other languages:\n- [remote-terminal](../remote-terminal/) -- Go agent + TUI client\n- [remote-terminal-node](../remote-terminal-node/) -- Node.js agent\n\n## Getting Started\n1. Download or clone this blueprint to the target machine\n2. Requires Python 3.10+\n3. Install the package:\n   ```\n   pip install -e .\n   ```\n4. Run the agent:\n   ```\n   remote-terminal-agent --token <YOUR_TOKEN> --apikey <YOUR_API_KEY> --appid <YOUR_APP_ID>\n   ```\n5. Open the web client and connect using the device ID\n\n## Architecture\n- Async Python (asyncio) with websocket-based NoLag client\n- Subscribes to the `commands` topic on a room named after the device ID\n- Executes commands locally using `subprocess` or PTY for interactive sessions\n- Streams PTY output back via the `session_output` topic\n- Broadcasts device status every 30 seconds on the `status` topic\n- Auto-creates the room via the NoLag REST API on startup\n\n## NoLag Features Used\n- **Pub/Sub messaging** for command/response exchange\n- **Room-per-device** with dynamic room creation via REST API\n- **Reconnection** for resilient long-running agent connections\n- **Binary-safe messaging** via base64 encoding for PTY data"
}
